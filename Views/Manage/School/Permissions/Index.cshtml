@model PermissionsPageVm
@{
    ViewData["Title"] = "Permissions";
}
<h1 class="mg-page-title">Permissions</h1>
<p class="mg-page-sub">Assign module access for additional administrators</p>

<div class="mg-card" style="margin-bottom:1rem;">
    <form method="get" class="mg-form" style="max-width:420px;">
        <div class="mg-field">
            <label>Administrator</label>
            <select name="userId" onchange="this.form.submit()">
                @foreach (var a in Model.Admins)
                {
                    <option value="@a.UserId" selected="@(a.UserId == Model.SelectedUserId)">@a.FullName (@(a.IsPrimary ? "Primary" : a.AdminType))</option>
                }
            </select>
        </div>
    </form>
</div>

@if (string.IsNullOrEmpty(Model.SelectedUserId))
{
    <div class="mg-card"><div class="mg-empty"><h3>No admins</h3><p>Create an additional admin first.</p></div></div>
}
else
{
    var selected = Model.Admins.FirstOrDefault(a => a.UserId == Model.SelectedUserId);
    if (selected?.IsPrimary == true)
    {
        <div class="mg-card"><p>The primary school admin always has full access to every module.</p></div>
    }
    else
    {
        <div class="mg-card">
            <form method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="userId" value="@Model.SelectedUserId" />
                <partial name="Partials/_PermissionPicker" model="@(new PermissionPickerVm { FieldName = "permissionCodes", SelectedCodes = Model.GrantedCodes })" />
                <button type="submit" class="mg-btn mg-btn-primary" style="margin-top:1.5rem;">Save permissions</button>
            </form>
        </div>
    }
}
